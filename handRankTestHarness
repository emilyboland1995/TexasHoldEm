
public class test1 {

	public static void main(String[] args) {
		//new Game();
		//**test handRank class
		Deck decks = new Deck();
		Card[] holeCards = new Card[2];
		Card[] boardCards = new Card[4];
		int handValue = 0;
//		while (handValue < 9000000){
			decks.resetDeck();
			decks.shuffleDeck();
			holeCards[0] = decks.drawCard();
			holeCards[1] = decks.drawCard();
			boardCards[0] = decks.drawCard();
			boardCards[1] = decks.drawCard();
			boardCards[2] = decks.drawCard();
			boardCards[3] = decks.drawCard();
//			boardCards[4] = decks.drawCard();
			handRank myHand = new handRank(holeCards,boardCards);
			handValue = myHand.handValue;
//		}
	}
}
